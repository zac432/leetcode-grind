#this is a sorted linked list 

note: 
linked lists:
- don’t have indexes
- don’t know their length

For Python lists → you can use both for and while.
For Linked Lists → normally use while, because you must “walk” node by node with .next.

then we  
